# NewsCollectorAgent – Python dependencies
# Pinned for reproducibility; optimised for low-memory deployment

# RSS parsing
feedparser==6.0.11

# HTTP client (used instead of feedparser's urllib for timeout control)
requests==2.32.3
urllib3==2.2.3

# PostgreSQL
psycopg2-binary==2.9.10

# Scheduler
APScheduler==3.10.4

# Environment config
python-dotenv==1.0.1

# API & Watchlist
yfinance==0.2.54
Flask==3.0.3
